/*
import { describe, it, expect } from 'jest-lite';
import sum from '../../sum';

describe('487805233 sum', () => {
  it('487805233 should return sum of arguments', () => {
    expect(sum(1, 2)).toBe(3);
  });
});

describe('487805233 sum', () => {
  it('487805233 should return sum of arguments', () => {
    expect(sum(1, 2)).toBe(4);
  });
});
*/
